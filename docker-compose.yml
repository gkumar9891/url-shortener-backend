version: '3'
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    command: sleep infinity
    volumes:
      - .:/home/app

  mysql:
    image: mysql
    restart: always
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: passphrase
    volumes:
      - sql_data:/var/lib/mysql

volumes:
  sql_data: